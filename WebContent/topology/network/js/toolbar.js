// JavaScript Document
/**
 * 兼容ie和ff
 * 当做时间函数调用，没有传入event时间对象，因此event通过arguments来获取
 * @author HP
 */
function buttonNewOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_new_out";
}
function buttonNewOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_new_over";
}

function buttonEditMapOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_editmap_out";
}
function buttonEditMapOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_editmap_over";
}
function buttonSearchOut() {
	
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_search_out";
}
function buttonSearchOver() {
	
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_search_over";
}
function buttonShowTreeOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_showtree_out";
}
function buttonShowTreeOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_showtree_over";
}
function buttonOpenOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_open_out";
}
function buttonOpenOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_open_over";
}


function buttonEditOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_edit_out";
}
function buttonEditOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_edit_over";
}


function buttonSaveOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_save_out";
}
function buttonSaveOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_save_over";
}

function buttonSelectOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_select_out";
}
function buttonSelectOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_select_over";
}


function buttonRefreshOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_refresh_out";
}
function buttonRefreshOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_refresh_over";
}


function buttonFullscreenOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_fullscreen_out";
}
function buttonFullscreenOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_fullscreen_over";
}


function buttonViewOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_view_out";
}

function buttonViewOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_view_over";
}

function buttonCreate1Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create1_out";
}

function buttonCreate1Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create1_over";
}

function buttonCreate2Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create2_out";
}

function buttonCreate2Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create2_over";
}

function buttonCreate3Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create3_out";
}

function buttonCreate3Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create3_over";
}

function buttonCreate4Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create4_out";
}

function buttonCreate4Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create4_over";
}
function buttonCreate5Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create5_out";
}

function buttonCreate5Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create5_over";
}
function buttonCreate6Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create6_out";
}

function buttonCreate6Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create6_over";
}
function buttonCreate7Out() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create7_out";
}

function buttonCreate7Over() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_create7_over";
}
function buttonControllerOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_controller_out";
}
function buttonControllerOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_controller_over";
}


function buttonBrowseOut() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_browse_out";
}
function buttonBrowseOver() {
	var event = window.event || arguments.callee.caller.event;
	var srcElement = event.srcElement || event.target;
	srcElement.className="button_browse_over";
}
